services:
  orchestrator:
    build: .
    container_name: orchestrator
    ports:
      - "${PORT:-5550}:5550"
    env_file:
      - .env
    volumes:
      - backups:/backups
    networks:
      - backups_net
volumes:
  backups:
networks:
  backups_net:
    driver: bridge
    name: backups_net
